# 源码说明

本项目源码位于项目根目录，包括：

- `src/`：React 前端源码
- `scripts/`：Excel 数据转换脚本
- `public/media/`：从 `associatedMedia` 缓存的观察图片
- `package.json`：运行脚本和依赖说明

由于这是一个 Vite 前端项目，提交时建议保留整个项目文件夹，而不是只提交本 `source/` 文件夹。

